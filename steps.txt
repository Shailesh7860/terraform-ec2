curl -fsSL https://floci.io/install.sh | sh

curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
unzip awscliv2.zip
sudo ./aws/install

eval $(floci env)

floci start

docker rm -f floci

docker network create floci-net

docker run -d --name floci --network floci-net -p 4566:4566 floci/floci:latest

docker run -d --name floci-ui --network floci-net -p 4500:4500 -e FLOCI_ENDPOINT=https://studious-space-yodel-wqx6vrrxwpfgpwx-4566.app.github.dev/ -e AWS_DEFAULT_REGION=ap-south-1 floci/floci-ui:latest

wget -O - https://apt.releases.hashicorp.com/gpg | sudo gpg --dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gpg
echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(grep -oP '(?<=UBUNTU_CODENAME=).*' /etc/os-release || lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list
sudo apt update && sudo apt install terraform



=========================================================================================
Test

aws ec2 describe-instances --endpoint-url=https://studious-space-yodel-wqx6vrrxwpfgpwx-4566.app.github.dev/ --region ap-south-1
aws sts get-caller-identity
